{% extends 'layout.html.twig' %}

{% block title %}AG Produtos || Excluir Tag {% endblock %}

{% block content %}
    <header class="container">
        <h1>Excluir Tag</h1>
    </header>

    <section class="container">
        <form class="form-horizontal" method="post"
              action="{{ path('tags-deletar', {'id': tag.id}) }}">
            <input type="hidden" id="id" name="id" value="{{ tag.id }}">
            <div class="form-group">
                <label for="nome" class="col-sm-2 control-label">Nome</label>
                <div class="col-sm-10">
                    <input type="text" class="form-control" name="nome" disabled="disabled" value="{{tag.nome}}">
                </div>
            </div>
            <div class="form-group">
                <div class="col-sm-offset-2 col-sm-10">
                    <button type="submit" class="btn btn-info"><i class="glyphicon glyphicon-check"></i> Confirmar</button>
                    <a href="{{ path('tags') }}" class="btn btn-default">Cancelar</a>
                </div>
            </div>
        </form>
    </section>
{% endblock %}

{% block form %}

{% endblock %}

{% block classhome %}{% endblock %}
{% block classprodutos %}{% endblock %}
{% block classcategorias %}{% endblock %}
{% block classtags %}active{% endblock %}